[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "metarouting"
version = "0.1.0"
description = "Research benchmarks for agentic meta-routing"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "pydantic>=2.0",
    "rich>=13.0",
    "tqdm>=4.65",
]

[project.optional-dependencies]
dev = ["matplotlib>=3.8", "pypdf>=5.0", "pytest>=7.0"]

[project.scripts]
metarouter-benchmark = "metarouter.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
