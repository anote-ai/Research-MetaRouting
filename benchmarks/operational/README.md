# Operational Benchmark

The DAI benchmark is generated deterministically by
`src/metarouter/workloads.py`. It contains balanced data-analysis, research, and
document-processing profiles with explicit routing-need annotations.

These profiles support controlled policy diagnostics but are not live tasks and
must not be reused as the primary evidence for the AAAI paper.
